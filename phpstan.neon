includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    paths:
        - src
    level: 8
    ignoreErrors:
        -
            # Consumer-facing trait (apps `use HasRoles` on their User model); never used inside src.
            identifier: trait.unused
            path: src/Traits/HasRoles.php
